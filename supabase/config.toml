# Projeto local / CI — alinhe major_version com o Postgres do painel (Database → Settings).
# Ref remoto: rixzxbsareoyfvjmeavu (https://rixzxbsareoyfvjmeavu.supabase.co)
# Docs: https://supabase.com/docs/guides/deployment/database-migrations

project_id = "carteira_adtc"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
# Ajuste se o painel mostrar outra versão (Settings → Database).
major_version = 15

[studio]
enabled = true
port = 54323

[inbucket]
enabled = false
port = 54324

[storage]
enabled = true
file_size_limit = "50MiB"

[auth]
enabled = true
site_url = "http://127.0.0.1:3000"
